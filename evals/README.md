# Evals

This directory contains fast checks that make repository behavior legible to agents.

- `smoke_eval.py`: verifies the harness scaffold exists.
- Add task-specific evals for model, agent, UI, data, or research workflows.
