# 场景优先版 MVP 技术落地方案：AI 情绪护盾

## 1. 核心取舍

- [必须] Demo 重点从“系统有多少模块”改成“一个高压服务场景如何被 AI 双向降噪”。
- [必须] 所有功能围绕输入输出闭环设计：客户输入、AI 输出给服务者、服务者输入、AI 输出给客户。
- [必须] 语音输入纳入演示，但只作为输入方式，不做完整语音客服系统。
- [必须] 语音转写失败时，自动回退到文本输入或预置脚本文案。
- [以后再做] 语音情绪识别、实时打断、TTS 播报、多人通话、电话系统接入。

一句话成功标准：评委看到客户用语音或文字发出高情绪投诉，系统把它转成服务者可处理的信息，服务者再把草稿优化成专业回复并手动发送。

## 2. 演示场景设计

### 场景 A：暴怒催单

- [必须] 客户输入：
  - 文字：`你们到底会不会送？半小时了还没到！再不来我就投诉退款！`
  - 语音：客户点击录音，说出同样内容。
- [必须] AI 给服务者输出：
  - 过滤表达：`客户对配送延迟非常不满，并提出投诉和退款诉求。`
  - 核心诉求：`查询订单进度；说明延迟原因；给出处理方案。`
  - 情绪等级：`angry`
  - 情绪电量：`100 -> 88`
  - 建议回复：`我先帮您核实订单状态，并尽快给您明确的处理结果。`
  - 原文：服务者点击后可见。
- [必须] 服务者输入：
  - 草稿：`我帮你看一下，别急。`
- [必须] AI 给客户输出：
  - 优化后：`我会立即帮您核实订单状态，并尽快给您明确的处理结果。`

### 场景 B：退款威胁

- [必须] 客户输入：
  - `你们服务太差了，我现在就要退款，不处理我就差评。`
- [必须] AI 给服务者输出：
  - 过滤表达：`客户对服务体验不满，要求退款，并提到差评风险。`
  - 核心诉求：`确认退款规则；说明可处理路径；安抚但不承诺。`
  - 情绪等级：`angry`
  - 风险标签：`退款、差评`
- [必须] 服务者输入：
  - `退款要看规则，不是我说退就退。`
- [必须] AI 给客户输出：
  - `退款需要根据订单状态和平台规则确认。我会先帮您核实是否符合退款条件，并给您明确答复。`

### 场景 C：极端辱骂

- [可选但建议演示] 客户输入：
  - `你们都是废物吗？这点事都做不好？`
- [必须] AI 给服务者输出：
  - 过滤表达：`客户使用了攻击性表达，对处理效率极度不满。`
  - 核心诉求：`要求尽快处理当前问题。`
  - 情绪等级：`extreme`
  - 情绪电量：明显下降，触发休息提醒。
- [必须] 亮点：
  - 不把辱骂直接推给服务者。
  - 但服务者可查看原文。
  - 系统不替服务者判断客户对错。

## 3. 输入输出总表

| 环节 | 输入方 | 输入 | 处理 | 输出方 | 输出 |
|---|---|---|---|---|---|
| 下行语音 | 客户 | 麦克风语音 | 转写成文本 | 后端 | `transcript` |
| 下行文字 | 客户 | 原始投诉文本 | 下行情绪翻译 | 服务者 | 过滤文本、诉求、情绪等级、建议回复 |
| 原文查看 | 服务者 | 点击查看原文 | 展开原始消息 | 服务者 | 客户原文 |
| 情绪电量 | 系统 | 情绪等级 | 扣分算法 | 服务者 | 电量条、休息提醒 |
| 上行草稿 | 服务者 | 口语化回复 | 上行话术优化 | 服务者 | 优化候选回复 |
| 人工确认 | 服务者 | 点击确认发送 | 广播正式消息 | 客户 | 客户可见回复 |

## 4. 语音输入 MVP 方案

- [必须] 客户端支持“按住/点击录音”按钮。
- [必须] 录音结果先转成文字，再走原有 `customer_send` 流程。
- [必须] 转写文本在客户输入框中可编辑，客户点击发送后才提交。
- [必须] 现场兜底：录音失败时，一键填入预置脚本文案。
- [可选] 服务者端也支持语音草稿，但优先级低于客户语音输入。
- [以后再做] TTS 把客服回复读给客户听。

推荐实现优先级：

1. [必须] 浏览器 Web Speech API：最快，前端完成，不依赖后端上传音频。
2. [可选] Whisper API：更稳定但需要 key、上传音频、处理权限和延迟。
3. [必须] Mock transcript：现场按钮直接生成转写结果，保证可演。

## 5. WebSocket 事件按输入输出重排

### `customer_voice_transcribed`

- [必须] 发出方：客户前端本地或后端 Whisper。
- Payload：

```json
{
  "audioMode": "browser_speech",
  "transcript": "你们到底会不会送？半小时了还没到！再不来我就投诉退款！",
  "confidence": 0.86
}
```

- 前端处理：填入输入框，不直接发送。

### `customer_send`

- [必须] 发出方：客户前端。
- Payload：

```json
{
  "inputMode": "voice",
  "text": "你们到底会不会送？半小时了还没到！再不来我就投诉退款！"
}
```

### `customer_translated`

- [必须] 发出方：后端。
- Payload：

```json
{
  "originalText": "你们到底会不会送？半小时了还没到！再不来我就投诉退款！",
  "filteredText": "客户对配送延迟非常不满，并提出投诉和退款诉求。",
  "coreRequest": "查询订单进度；说明延迟原因；给出处理方案。",
  "emotionLevel": "angry",
  "emotionScore": 4,
  "riskFlags": ["催单", "投诉", "退款"],
  "suggestedReplies": [
    {
      "tone": "firm",
      "text": "我先帮您核实订单状态，并尽快给您明确的处理结果。"
    }
  ],
  "originalVisible": true,
  "approvalNeeded": true
}
```

### `agent_draft`

- [必须] 发出方：服务者前端。
- Payload：

```json
{
  "inputMode": "text",
  "text": "我帮你看一下，别急。"
}
```

### `agent_polished`

- [必须] 发出方：后端。
- Payload：

```json
{
  "polishedText": "我会立即帮您核实订单状态，并尽快给您明确的处理结果。",
  "tone": "firm",
  "approvalNeeded": true,
  "originalVisible": true
}
```

### `agent_approved`

- [必须] 发出方：服务者前端。
- Payload：

```json
{
  "text": "我会立即帮您核实订单状态，并尽快给您明确的处理结果。"
}
```

## 6. 页面重点改造

### 客户端页面

- [必须] 普通聊天区。
- [必须] 文本输入框。
- [必须] 语音输入按钮：
  - 状态：`idle | recording | transcribing | transcript_ready | failed`
  - 录音成功：转写结果进入输入框。
  - 录音失败：显示“使用演示语音文本”按钮。
- [必须] 发送按钮。
- [可选] 三个场景脚本按钮。

### 服务者端页面

- [必须] “AI 降噪结果”区域：
  - 过滤后文本。
  - 核心诉求。
  - 情绪等级。
  - 风险标签。
- [必须] “查看原文”按钮。
- [必须] “建议回复”区域。
- [必须] 情绪电量条。
- [必须] 服务者草稿输入框。
- [必须] “AI 优化”按钮。
- [必须] “确认发送”按钮。
- [可选] 服务者语音输入草稿按钮。

## 7. Prompt 输入输出要求

### 下行 Prompt 输出

- [必须] 输入：客户原文、输入方式、上下文。
- [必须] 输出 JSON：
  - `filtered_text`
  - `core_request`
  - `emotion_level`
  - `emotion_score`
  - `risk_flags`
  - `suggested_replies`
  - `original_visible: true`
  - `approval_needed: true`

### 上行 Prompt 输出

- [必须] 输入：服务者草稿、客户核心诉求、客户情绪等级。
- [必须] 输出 JSON：
  - `polished_text`
  - `tone`
  - `changes_summary`
  - `missing_info`
  - `original_visible: true`
  - `approval_needed: true`

## 8. 最小开发顺序调整

- 第 0-2 小时：[必须] 先打通文字输入输出闭环。
- 第 2-4 小时：[必须] 做场景 A 的服务者输出卡片，不追求多场景。
- 第 4-6 小时：[必须] 接上行优化和人工确认发送。
- 第 6-7 小时：[必须] 加情绪电量和查看原文。
- 第 7-8 小时：[必须] 加客户语音输入按钮，优先 Web Speech API。
- 第 8-9 小时：[必须] 加 mock transcript 和预置脚本兜底。
- 第 9-12 小时：[必须] 反复彩排场景 A；场景 B/C 只作为备用按钮。

## 9. 关键实现默认

- [必须] 默认只做客户语音输入，不做客服语音回复。
- [必须] 语音转写结果不自动发送，必须客户点击发送。
- [必须] 后端不区分语音和文字处理逻辑，只接收最终文本。
- [必须] `inputMode` 只用于 UI 标记和演示说明。
- [必须] 现场演示时先试语音，失败立即点预置脚本，不解释技术故障太久。

## 10. 不要做清单

- 不要做完整语音通话。
- 不要做实时语音流式转写。
- 不要做 TTS。
- 不要做声纹、语速、音量情绪分析。
- 不要让语音链路成为唯一入口。
- 不要为了语音牺牲文字闭环稳定性。
- 不要加复杂业务后台。
- 不要做多客户、多客服、多房间。