{
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "python -m uvicorn app.main:app --app-dir backend --reload --host 0.0.0.0 --port 8000",
    "check:harness": "python evals/smoke_eval.py && python -m unittest discover -s tests -p \"test_*.py\"",
    "check:backend": "python -m unittest discover -s backend/tests -p \"test_*.py\"",
    "check:frontend": "npm --prefix frontend run lint",
    "check": "npm run check:harness && npm run check:backend && npm run check:frontend"
  }
}
